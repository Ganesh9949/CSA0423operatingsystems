#include <stdlib.h>

int main() {
    int returnVal = system("ls -l");
    
    if (returnVal == -1) {
        printf("Failed to execute the command.\n");
        return 1;
    }
    
    return 0;
}
